plugins {
    id "org.sonarqube" version "4.4.1.3373"
}

sonarqube {
    properties {
        property "sonar.projectKey", "GS"
        property "sonar.projectName", "gs-gradle"
        property "sonar.java.binaries", "build/classes/java/main"
    }
}
